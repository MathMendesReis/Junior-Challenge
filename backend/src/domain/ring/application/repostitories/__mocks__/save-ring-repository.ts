import type { SaveRingRepository } from "../save-ring-repository";
import { mock } from 'vitest-mock-extended';

const mockSaveRingRepository = mock<SaveRingRepository>();

export {mockSaveRingRepository}
